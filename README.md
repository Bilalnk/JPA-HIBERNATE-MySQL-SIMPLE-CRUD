# JPA-HIBERNATE-MySQL-SIMPLE-CRUD
Simple CRUD operations were performed with JPA-Hibernate using MySQL database.

This repo contains simple CRUD proccesses with using JPA HIBERNATE on MySQL database. 


